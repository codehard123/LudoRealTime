class LudoToken {
  constructor(circle, color) {
    this.HousePos= 
    this.inHouse = true;
    this.OutofGame = false;
    this.isOverlapping = false;
    this.startPoint=Ludo.prototype.findStartPos(this.color);
    
  }
}
Ludo.prototype.moveToken = function () {

};
Ludo.prototype.findStartPos=function(color){
    switch(color)
    {
        case "red":
        case "yellow":
        case "green":
        case "blue":
    }
}
Ludo.prototype.reset=function(object){
    object.x=
}
